# Debate Bot Configuration
# This file controls agent behavior and model selection across the application

# Agent model configuration
agents:
  # Case generation agent - used for creating debate cases
  case_generator:
    model: claude-sonnet-4-5  # High-quality output for competitive cases

  # Research agent - used for cutting evidence cards
  research:
    model: claude-haiku-4-5  # Cost-effective for iterative research

  # Specialized prep agents (parallel prep system)
  prep_strategy:
    model: claude-haiku-4-5  # Task generation is straightforward enumeration
  prep_search:
    model: claude-haiku-4-5  # Just generates queries (~20 tokens)
  prep_cutter:
    model: claude-haiku-4-5  # Identifies cut points (~50 tokens)
  prep_organizer:
    model: claude-haiku-4-5  # Places cards, generates feedback

# API configuration
api:
  max_tokens: 4096  # Maximum tokens per response
